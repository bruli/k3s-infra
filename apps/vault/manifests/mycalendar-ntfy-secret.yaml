apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mycalendar-ntfy-secret
  namespace: mycalendar
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: ntfy-secret
    creationPolicy: Owner
  data:
    - secretKey: user
      remoteRef:
        key: ntfy
        property: user
    - secretKey: password
      remoteRef:
        key: ntfy
        property: password