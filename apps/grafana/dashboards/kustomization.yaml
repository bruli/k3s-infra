---
namespace: monitoring

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: grafana-dashboards
    files:
      - grafana/dashboards/assets/cluster.json
      - grafana/dashboards/assets/pinger.json
