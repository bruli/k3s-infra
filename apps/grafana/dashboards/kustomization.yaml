namespace: monitoring

generatorOptions:
  disableNameSuffixHash: true
  labels:
    grafana_dashboard: "1"   # <-- ha de quadrar amb sidecar.dashboards.label
  annotations:
    k8s-sidecar-target-directory: "/var/lib/grafana/dashboards"

configMapGenerator:
  - name: grafana-dashboards
    files:
      - cluster.json
      - pinger.json
